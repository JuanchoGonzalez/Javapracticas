#include <stdio.h>
#include "pilas.h"	

int main( int argc, char * argv[] )
{
  
    tipo_pila pila = crear();
	
	apilar(pila, 1);
	apilar(pila, 2);
	apilar(pila, 3);
	apilar(pila, 4);
	*/
	// pila = [ 1, 2, 3, 4 ]
	
	printf( "pila = " );
	printf("\n");
	
  return 0;
}